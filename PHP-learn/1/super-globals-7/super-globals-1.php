<?php

echo "<pre>";  // for better array show
var_dump($GLOBALS);  //we have 4 super-globals : $_POST - $_GET - $_FILES - $_COOKIE
echo "</pre>";
//[w] we can use the  super-globals with $_...    like :
 $_COOKIE;
 $_POST;
 $_GET;
 $_FILES;

 